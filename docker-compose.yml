services:
  db:
    image: mysql:8.0
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: bukalapang_db
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: